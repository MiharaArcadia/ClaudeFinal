package com.blockzy.game

import io.flutter.embedding.android.FlutterActivity

/**
 * The single Flutter host activity. Kept intentionally minimal — all game logic
 * lives in Dart. iOS uses the standard Runner, so porting needs no changes here.
 */
class MainActivity : FlutterActivity()
